<form method="post" action="{{ path('app_reclam_delete', {'idr': reclam.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclam.idr) }}">
    <button class="btn btn-primary">Delete</button>


</form>
